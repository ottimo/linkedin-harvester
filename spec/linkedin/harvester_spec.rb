require "spec_helper"

describe Linkedin::Harvester do
  it "has a version number" do
    expect(Linkedin::Harvester::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
